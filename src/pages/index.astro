---
import Base from '../layouts/Base.astro';
import MusicPlayer from '../components/MusicPlayer.astro';
import Img from '../components/Img.astro';
import Rsvp from '../components/Rsvp.astro';
import {
  groom,
  bride,
  monogram,
  verse,
  events,
  photos,
  weddingDateISO,
  weddingDateLabel,
} from '../data/wedding';

const nav = [
  { id: 'moi', label: 'Thiệp mời' },
  { id: 'chuyen', label: 'Chuyện tình' },
  { id: 'sukien', label: 'Sự kiện' },
  { id: 'album', label: 'Khoảnh khắc' },
  { id: 'rsvp', label: 'Tham dự' },
];
// Nhóm theo "set" chụp cho gọn mắt (OCD-friendly):
const gallery = [
  ...photos.classic, // set cổ điển villa (vest đen · váy xoè)
  photos.editorial[0], photos.editorial[1], photos.editorial[3], // editorial vest trắng: concept-9, 12, 14
  photos.editorial[2], photos.editorial[4], photos.editorialWide[0], // editorial vest đen / skyline: concept-13, 4, +8 (mới)
  ...photos.mono, photos.monoWide[1], // đen trắng: concept-15, 16, +21 (mới)
];
---

<Base
  title="Minh Luân & Thu Trang — Thiệp cưới 08.08.2026"
  description="Trân trọng kính mời bạn đến chung vui trong ngày lễ thành hôn của Minh Luân & Thu Trang — 08.08.2026 tại Nhà thờ Phanxicô Xaviê, Thủ Đức, Sài Gòn."
  path="/"
  image="/og-image.jpg"
  themeColor="#4a0d16"
  bodyClass="ea"
>
  <!-- Chạy sớm trong <head>: đã có tên lưu → ẩn vỏ thiệp ngay, tránh nháy -->
  <script is:inline slot="head">
    try {
      if (localStorage.getItem('guestName')) document.documentElement.classList.add('skip-cover');
    } catch (e) {}
  </script>

  <!-- VỎ THIỆP / MÀN HÌNH MỞ THIỆP -->
  <div class="cover" id="cover">
    <div class="cover-inner">
      <span class="cover-cross" aria-hidden="true"></span>
      <div class="cover-mono">{monogram}</div>
      <p class="cover-kicker">Trân trọng kính mời</p>
      <h1 class="cover-names">Minh Luân <em>&amp;</em> Thu Trang</h1>
      <p class="cover-date">{weddingDateLabel} · Thủ Đức, Sài Gòn</p>
      <form class="cover-form" id="coverForm">
        <label class="cover-label" for="guestName">Thiệp mời thân gửi</label>
        <input
          id="guestName"
          name="guestName"
          type="text"
          placeholder="Nhập tên của bạn…"
          autocomplete="name"
          maxlength="60"
        />
        <button type="submit" class="cover-open">Mở thiệp ✦</button>
      </form>
    </div>
  </div>

  <!-- Điều hướng dọc tối giản -->
  <nav class="side-nav" aria-label="Mục lục">
    <a class="brand" href="#top">{monogram}</a>
    <ul>
      {nav.map((n) => (
        <li><a href={`#${n.id}`}>{n.label}</a></li>
      ))}
    </ul>
    <span class="side-date">{weddingDateLabel}</span>
  </nav>

  <main id="top">
    <!-- HERO — editorial split, chữ serif lớn + ảnh full-bleed -->
    <header class="hero">
      <div class="hero-copy">
        <p class="guest-greet" id="guestGreet" hidden></p>
        <p class="eyebrow">Trân trọng báo tin · Save the date</p>
        <h1>
          <span class="n1">Minh Luân</span>
          <span class="amp">&amp;</span>
          <span class="n2">Thu Trang</span>
        </h1>
        <div class="hero-meta">
          <span>{weddingDateLabel}</span>
          <span class="dot">✦</span>
          <span>Thủ Đức · Sài Gòn</span>
        </div>
        <p class="hero-note">
          Micae Nguyễn Võ Minh Luân — Maria Nguyễn Thị Thu Trang.
          Một lời mời trân trọng đến buổi tiệc chung vui của chúng tôi.
        </p>
      </div>
      <figure class="hero-fig">
        <Img src={photos.classic[0]} alt="Minh Luân và Thu Trang" loading="eager" fetchpriority="high" />
        <figcaption>№ 01 — L&amp;T</figcaption>
      </figure>
      <div class="scroll-cue">Cuộn xuống <span>↓</span></div>
    </header>

    <!-- CÂU KINH THÁNH -->
    <section class="verse reveal" id="moi">
      <div class="cross" aria-hidden="true"></div>
      <blockquote>“{verse.text}”</blockquote>
      <cite>— {verse.ref}</cite>
    </section>

    <!-- CHUYỆN TÌNH — editorial xen kẽ -->
    <section class="story" id="chuyen">
      <div class="story-head reveal">
        <span class="tag">Chương một</span>
        <h2>Chúng mình<br /><em>về chung một nhà</em></h2>
      </div>

      <div class="story-row reveal">
        <figure class="sr-img"><Img src={photos.editorial[0]} alt="Chú rể Minh Luân" /></figure>
        <div class="sr-text">
          <span class="num">01</span>
          <h3>Chú rể</h3>
          <p class="who">{groom.saintName} {groom.fullName}</p>
          <p class="fam">{groom.role} · {groom.father} &amp; {groom.mother}</p>
          <p class="place">{groom.hometown}</p>
        </div>
      </div>

      <div class="story-row alt reveal">
        <figure class="sr-img"><Img src={photos.classic[3]} alt="Cô dâu Thu Trang" /></figure>
        <div class="sr-text">
          <span class="num">02</span>
          <h3>Cô dâu</h3>
          <p class="who">{bride.saintName} {bride.fullName}</p>
          <p class="fam">{bride.role} · {bride.father} &amp; {bride.mother}</p>
          <p class="place">{bride.hometown}</p>
        </div>
      </div>
    </section>

    <!-- ĐẾM NGƯỢC -->
    <section class="count reveal">
      <p class="count-lead">Đếm ngược đến ngày hạnh phúc</p>
      <div class="count-grid" id="cd-a">
        <div><b data-d>00</b><span>Ngày</span></div>
        <div><b data-h>00</b><span>Giờ</span></div>
        <div><b data-m>00</b><span>Phút</span></div>
        <div><b data-s>00</b><span>Giây</span></div>
      </div>
    </section>

    <!-- SỰ KIỆN -->
    <section class="events" id="sukien">
      <div class="events-head reveal">
        <span class="tag">Chương hai</span>
        <h2>Lịch trình<br /><em>hôn lễ</em></h2>
      </div>
      <div class="ev-grid">
        {events.map((e, i) => (
          <article class="ev-card reveal" style={`transition-delay:${i * 90}ms`}>
            <span class="ev-side">{e.side}</span>
            <h3>{e.title}</h3>
            {e.time && <p class="ev-time">{e.time}{e.reception ? ` · ${e.reception}` : ''}</p>}
            <p class="ev-date">{e.weekday ? `${e.weekday}, ` : ''}{e.date}</p>
            {e.lunar && <p class="ev-lunar">{e.lunar}</p>}
            <div class="ev-rule"></div>
            <p class="ev-venue">{e.venue}</p>
            <p class="ev-addr">{e.address}</p>
            <a
              class="ev-map"
              href={e.mapUrl ?? `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(e.mapQuery)}`}
              target="_blank"
              rel="noopener">Xem bản đồ →</a>
          </article>
        ))}
      </div>
    </section>

    <!-- ALBUM — slider vuốt ngang -->
    <section class="album" id="album">
      <div class="album-head reveal">
        <span class="tag">Chương ba</span>
        <h2>Khoảnh khắc</h2>
        <p class="album-hint">Vuốt ngang để xem thêm →</p>
      </div>

      <div class="slider reveal">
        <button class="slider-nav prev" type="button" aria-label="Ảnh trước">‹</button>
        <div class="slider-track" id="albumTrack">
          {gallery.map((src, i) => (
            <figure class="slide">
              <Img src={src} alt={`Ảnh cưới ${i + 1}`} draggable={false} />
            </figure>
          ))}
        </div>
        <button class="slider-nav next" type="button" aria-label="Ảnh sau">›</button>
      </div>

      <div class="slider-progress" aria-hidden="true"><span id="albumBar"></span></div>
    </section>

    <!-- SỰ HIỆN DIỆN / RSVP -->
    <section class="rsvp" id="rsvp">
      <div class="rsvp-inner reveal">
        <span class="tag">Sự hiện diện của bạn</span>
        <h2>Chung vui<br /><em>cùng chúng mình</em></h2>
        <p class="rsvp-lead">
          Đám cưới sẽ trọn vẹn hơn khi có bạn. Hãy xác nhận tham dự để gia đình
          chúng mình chuẩn bị đón tiếp thật chu đáo, và gửi đôi lời chúc phúc nhé.
        </p>
        <div class="rsvp-actions">
          <button type="button" class="rbtn" data-rsvp-open>Xác nhận tham dự</button>
          <button type="button" class="rbtn ghost" data-wish-open>Gửi lời chúc</button>
        </div>
        <p class="rsvp-note">
          Micae Minh Luân &amp; Maria Thu Trang — chờ đón bạn ngày {weddingDateLabel}
        </p>
      </div>
    </section>

    <!-- SỔ LƯU BÚT + MODAL RSVP/LỜI CHÚC -->
    <Rsvp />

    <!-- LỜI NGỎ / FOOTER -->
    <footer class="foot reveal">
      <div class="foot-mono">{monogram}</div>
      <p class="foot-thanks">
        Hẹn gặp Quý khách trong ngày trọng đại của chúng tôi.<br />
        Sự hiện diện của Quý khách là niềm vinh hạnh cho gia đình chúng tôi.
      </p>
      <p class="foot-names">Minh Luân &amp; Thu Trang</p>
      <p class="foot-date">{weddingDateLabel} · Thủ Đức, Sài Gòn</p>
    </footer>
  </main>

  <MusicPlayer variant="light" />

  <script is:inline define:vars={{ weddingDateISO }}>
    // Reveal on scroll
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((en) => {
          if (en.isIntersecting) {
            en.target.classList.add('in');
            io.unobserve(en.target);
          }
        });
      },
      { threshold: 0.15 }
    );
    document.querySelectorAll('.reveal').forEach((el) => io.observe(el));

    // VỎ THIỆP: nhập tên → mở thiệp → bật nhạc + cá nhân hoá
    (function () {
      const cover = document.getElementById('cover');
      const form = document.getElementById('coverForm');
      const input = document.getElementById('guestName');
      const greet = document.getElementById('guestGreet');
      if (!cover) return;

      let saved = null;
      try { saved = localStorage.getItem('guestName'); } catch (e) {}

      function showGreet(name) {
        if (greet && name) { greet.textContent = 'Thân mời ' + name; greet.hidden = false; }
      }

      // Đã có tên lưu sẵn → bỏ qua vỏ thiệp, vào thẳng thiệp
      if (saved) {
        showGreet(saved);
        document.documentElement.classList.add('skip-cover');
        cover.style.display = 'none';
        return;
      }

      // Chưa có tên → hiện vỏ thiệp, khoá cuộn
      document.body.style.overflow = 'hidden';

      function openCard(e) {
        if (e) e.preventDefault();
        const name = input ? input.value.trim() : '';
        if (name) {
          try { localStorage.setItem('guestName', name); } catch (e) {}
          showGreet(name);
        }
        cover.classList.add('open');
        document.body.style.overflow = '';
        // Bật nhạc nền (đúng lúc user tương tác → né chặn autoplay)
        const audio = document.getElementById('bgAudio');
        if (audio) { audio.play().catch(function () {}); }
        setTimeout(function () { cover.style.display = 'none'; }, 1300);
      }
      if (form) form.addEventListener('submit', openCard);
    })();

    // Countdown
    const target = new Date(weddingDateISO).getTime();
    const box = document.getElementById('cd-a');
    function tick() {
      const now = Date.now();
      let diff = Math.max(0, target - now);
      const d = Math.floor(diff / 86400000);
      diff -= d * 86400000;
      const h = Math.floor(diff / 3600000);
      diff -= h * 3600000;
      const m = Math.floor(diff / 60000);
      diff -= m * 60000;
      const s = Math.floor(diff / 1000);
      const p = (n) => String(n).padStart(2, '0');
      if (!box) return;
      box.querySelector('[data-d]').textContent = p(d);
      box.querySelector('[data-h]').textContent = p(h);
      box.querySelector('[data-m]').textContent = p(m);
      box.querySelector('[data-s]').textContent = p(s);
    }
    tick();
    setInterval(tick, 1000);

    // Slider "Khoảnh khắc" — vuốt ngang, kéo chuột, nút điều hướng
    (function () {
      const track = document.getElementById('albumTrack');
      const bar = document.getElementById('albumBar');
      const prev = document.querySelector('.slider-nav.prev');
      const next = document.querySelector('.slider-nav.next');
      if (!track) return;

      const step = () => {
        const s = track.querySelector('.slide');
        return s ? s.getBoundingClientRect().width + 18 : track.clientWidth * 0.8;
      };
      if (prev) prev.addEventListener('click', () => track.scrollBy({ left: -step() * 1.2, behavior: 'smooth' }));
      if (next) next.addEventListener('click', () => track.scrollBy({ left: step() * 1.2, behavior: 'smooth' }));

      // Kéo bằng chuột (desktop); mobile dùng cuộn cảm ứng native
      let down = false, sx = 0, sl = 0;
      track.addEventListener('pointerdown', (e) => {
        if (e.pointerType !== 'mouse') return;
        down = true; sx = e.clientX; sl = track.scrollLeft;
        track.classList.add('dragging');
        track.setPointerCapture(e.pointerId);
      });
      track.addEventListener('pointermove', (e) => {
        if (!down) return;
        track.scrollLeft = sl - (e.clientX - sx);
      });
      const up = () => { down = false; track.classList.remove('dragging'); };
      track.addEventListener('pointerup', up);
      track.addEventListener('pointercancel', up);

      function update() {
        const max = track.scrollWidth - track.clientWidth;
        const frac = max > 0 ? track.scrollLeft / max : 0;
        const w = Math.max(12, (track.clientWidth / track.scrollWidth) * 100);
        if (bar) { bar.style.width = w + '%'; bar.style.left = frac * (100 - w) + '%'; }
        if (prev) prev.toggleAttribute('disabled', track.scrollLeft < 4);
        if (next) next.toggleAttribute('disabled', track.scrollLeft > max - 4);
      }
      track.addEventListener('scroll', update, { passive: true });
      window.addEventListener('resize', update);
      update();
    })();
  </script>

  <style is:global>
    .ea {
      --wine: #6b1522;
      --wine-deep: #4a0d16;
      --cream: #f6f1e7;
      --paper: #fbf8f1;
      --gold: #b0863f;
      --ink: #2a1a14;
      background: var(--paper);
      color: var(--ink);
      font-family: var(--font-be);
    }
    main { overflow-x: clip; }

    /* Side nav */
    .side-nav {
      position: fixed; left: 0; top: 0; bottom: 0; width: 72px; z-index: 40;
      display: flex; flex-direction: column; align-items: center; justify-content: space-between;
      padding: 22px 0; mix-blend-mode: difference;
    }
    .side-nav .brand {
      font-family: var(--font-fraunces); font-size: 26px; letter-spacing: 1px;
      color: #fff; text-decoration: none;
    }
    .side-nav ul { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 20px; }
    .side-nav ul a {
      writing-mode: vertical-rl; text-orientation: mixed;
      font-size: 11px; letter-spacing: .28em; text-transform: uppercase;
      color: #fff; text-decoration: none; opacity: .85; transition: opacity .3s;
    }
    .side-nav ul a:hover { opacity: 1; }
    .side-date { writing-mode: vertical-rl; font-size: 10px; letter-spacing: .3em; color: #fff; opacity: .7; }

    /* HERO */
    .hero {
      position: relative; min-height: 100svh;
      display: grid; grid-template-columns: 1.05fr 0.95fr; align-items: center;
      padding-left: 72px;
    }
    .hero-copy { padding: 6vw 4vw 6vw 6vw; }
    .eyebrow {
      font-size: 12px; letter-spacing: .34em; text-transform: uppercase;
      color: var(--wine); margin: 0 0 26px;
    }
    .hero h1 { margin: 0; line-height: .92; font-weight: 400; }
    .hero h1 span { display: block; font-family: var(--font-fraunces); color: var(--wine-deep); }
    .hero h1 .n1, .hero h1 .n2 {
      font-size: clamp(3rem, 8vw, 6.6rem); font-optical-sizing: auto;
      font-style: italic; font-weight: 400;
    }
    .hero h1 .amp {
      font-size: clamp(1.4rem, 3vw, 2.4rem); color: var(--gold);
      margin: .1em 0 .1em .1em; font-style: italic;
    }
    .hero-meta {
      display: flex; align-items: center; flex-wrap: wrap; gap: 8px 16px; margin: 30px 0 24px;
      font-family: var(--font-fraunces); font-size: 18px; letter-spacing: .12em;
      text-transform: uppercase; color: var(--ink);
    }
    .hero-meta .dot { color: var(--gold); font-size: 12px; }
    .hero-note { max-width: 40ch; color: #6a5348; font-size: 15px; line-height: 1.85; font-weight: 300; }

    .hero-fig { position: relative; height: 100svh; margin: 0; overflow: hidden; }
    .hero-fig img { width: 100%; height: 100%; object-fit: cover; filter: saturate(.96) contrast(1.02); }
    .hero-fig::after {
      content: ''; position: absolute; inset: 0;
      background: linear-gradient(90deg, var(--paper) 0%, transparent 22%);
      pointer-events: none;
    }
    .hero-fig figcaption {
      position: absolute; bottom: 22px; right: 24px; color: #fff;
      font-family: var(--font-fraunces); font-style: italic; font-size: 15px; letter-spacing: .1em;
      text-shadow: 0 1px 10px rgba(0,0,0,.4);
    }
    .scroll-cue {
      position: absolute; bottom: 26px; left: calc(72px + 6vw);
      font-size: 11px; letter-spacing: .3em; text-transform: uppercase; color: var(--wine);
    }
    .scroll-cue span { display: inline-block; animation: bob 1.8s ease-in-out infinite; }
    @keyframes bob { 0%,100% { transform: translateY(0); } 50% { transform: translateY(6px); } }

    /* VERSE */
    .verse {
      text-align: center; padding: 12vh 8vw; max-width: 1000px; margin: 0 auto;
    }
    .cross {
      width: 1px; height: 46px; background: var(--gold); margin: 0 auto 34px; position: relative;
    }
    .cross::before {
      content: ''; position: absolute; top: 14px; left: -9px; width: 19px; height: 1px; background: var(--gold);
    }
    .verse blockquote {
      margin: 0; font-family: var(--font-fraunces); font-style: italic; font-weight: 300;
      font-size: clamp(1.5rem, 3.6vw, 2.8rem); line-height: 1.4; color: var(--wine-deep);
    }
    .verse cite { display: block; margin-top: 26px; font-style: normal; font-size: 12px; letter-spacing: .28em; text-transform: uppercase; color: var(--gold); }

    /* Shared section heads */
    .tag { font-size: 11px; letter-spacing: .34em; text-transform: uppercase; color: var(--gold); }
    .story-head h2, .events-head h2, .album-head h2 {
      font-family: var(--font-fraunces); font-weight: 400; color: var(--wine-deep);
      font-size: clamp(2.2rem, 5vw, 3.8rem); line-height: 1.02; margin: 14px 0 0;
    }
    .story-head em, .events-head em { font-style: italic; color: var(--gold); }

    /* STORY */
    .story { padding: 8vh 6vw 4vh calc(72px + 6vw); }
    .story-head { margin-bottom: 6vh; }
    .story-row {
      display: grid; grid-template-columns: 0.9fr 1.1fr; gap: clamp(24px, 5vw, 76px);
      align-items: center; margin-bottom: 10vh;
    }
    .story-row.alt { grid-template-columns: 1.1fr 0.9fr; }
    .story-row.alt .sr-img { order: 2; }
    .sr-img { margin: 0; overflow: hidden; aspect-ratio: 4/5; }
    .sr-img img { width: 100%; height: 100%; object-fit: cover; transition: transform 1.4s ease; }
    .story-row:hover .sr-img img { transform: scale(1.04); }
    .sr-text .num { font-family: var(--font-fraunces); font-style: italic; font-size: 40px; color: var(--gold); opacity: .5; }
    .sr-text h3 { font-family: var(--font-fraunces); font-size: 30px; font-weight: 400; margin: 4px 0 16px; color: var(--wine); }
    .sr-text .who { font-family: var(--font-fraunces); font-style: italic; font-size: 22px; color: var(--ink); margin: 0 0 10px; }
    .sr-text .fam { color: #6a5348; font-size: 14px; line-height: 1.7; font-weight: 300; margin: 0 0 4px; max-width: 34ch; }
    .sr-text .place { color: var(--gold); font-size: 13px; letter-spacing: .06em; }

    /* COUNTDOWN */
    .count { text-align: center; padding: 8vh 6vw; background: var(--cream); }
    .count-lead { font-size: 12px; letter-spacing: .32em; text-transform: uppercase; color: var(--wine); margin: 0 0 34px; }
    .count-grid { display: flex; justify-content: center; gap: clamp(20px, 6vw, 68px); }
    .count-grid div { display: flex; flex-direction: column; gap: 8px; }
    .count-grid b {
      font-family: var(--font-fraunces); font-weight: 300; font-size: clamp(2.4rem, 7vw, 4.6rem);
      color: var(--wine-deep); font-variant-numeric: tabular-nums; line-height: 1;
    }
    .count-grid span { font-size: 11px; letter-spacing: .3em; text-transform: uppercase; color: var(--gold); }

    /* EVENTS */
    .events { padding: 10vh 6vw 6vh calc(72px + 6vw); }
    .events-head { margin-bottom: 6vh; }
    .ev-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2px; background: #e7ddca; border: 1px solid #e7ddca; }
    .ev-card { background: var(--paper); padding: 40px 34px; transition: background .5s, transform 1s cubic-bezier(.22,1,.36,1), opacity 1s; }
    .ev-card:hover { background: #fffdf8; }
    .ev-side { font-size: 10px; letter-spacing: .3em; text-transform: uppercase; color: var(--gold); }
    .ev-card h3 { font-family: var(--font-fraunces); font-size: 27px; font-weight: 400; color: var(--wine); margin: 12px 0 14px; }
    .ev-time { font-family: var(--font-fraunces); font-style: italic; font-size: 19px; color: var(--ink); margin: 0 0 4px; }
    .ev-date { font-size: 14px; color: var(--ink); margin: 0; }
    .ev-lunar { font-size: 12px; color: #8a7565; font-style: italic; margin: 4px 0 0; }
    .ev-rule { height: 1px; background: linear-gradient(90deg, var(--gold), transparent); margin: 20px 0; }
    .ev-venue { font-weight: 500; font-size: 15px; margin: 0 0 4px; }
    .ev-addr { font-size: 13px; color: #6a5348; line-height: 1.6; margin: 0 0 18px; font-weight: 300; }
    .ev-map { font-size: 12px; letter-spacing: .12em; text-transform: uppercase; color: var(--wine); text-decoration: none; border-bottom: 1px solid var(--gold); padding-bottom: 3px; }
    .ev-map:hover { color: var(--gold); }

    /* ALBUM — slider vuốt ngang */
    .album { padding: 10vh 0; overflow: hidden; }
    .album-head { text-align: center; margin-bottom: 5vh; padding: 0 6vw; }
    .album-hint { margin-top: 16px; font-size: 12px; letter-spacing: .2em; text-transform: uppercase; color: var(--gold); }

    .slider { position: relative; }
    .slider-track {
      display: flex; gap: 18px; padding: 0 max(6vw, 40px);
      overflow-x: auto; scroll-snap-type: x mandatory; scroll-behavior: smooth;
      scrollbar-width: none; -ms-overflow-style: none; cursor: grab;
    }
    .slider-track::-webkit-scrollbar { display: none; }
    .slider-track.dragging { cursor: grabbing; scroll-behavior: auto; scroll-snap-type: none; }
    .slide {
      margin: 0; flex: 0 0 auto; width: clamp(260px, 33vw, 430px);
      aspect-ratio: 4 / 5; overflow: hidden; scroll-snap-align: start;
    }
    .slide img {
      width: 100%; height: 100%; object-fit: cover; filter: saturate(.97);
      transition: transform 1.2s ease, filter 1.2s ease; user-select: none; -webkit-user-drag: none;
    }
    .slide:hover img { transform: scale(1.05); filter: saturate(1.05); }

    .slider-nav {
      position: absolute; top: 50%; transform: translateY(-50%); z-index: 5;
      width: 52px; height: 52px; border-radius: 50%; cursor: pointer;
      border: 1px solid var(--gold); background: rgba(251, 248, 241, 0.82);
      color: var(--wine); font-family: var(--font-fraunces); font-size: 30px; line-height: 0;
      display: grid; place-items: center; backdrop-filter: blur(4px); padding-bottom: 5px;
      transition: background .3s, color .3s, opacity .3s;
    }
    .slider-nav:hover { background: var(--wine); color: var(--cream); }
    .slider-nav.prev { left: 16px; }
    .slider-nav.next { right: 16px; }
    .slider-nav[disabled] { opacity: .25; cursor: default; pointer-events: none; }

    .slider-progress { position: relative; height: 3px; width: min(240px, 60vw); margin: 34px auto 0; background: #e7ddca; border-radius: 3px; }
    .slider-progress span { position: absolute; top: 0; left: 0; height: 100%; width: 20%; border-radius: 3px; background: linear-gradient(90deg, var(--gold), var(--wine)); transition: left .15s ease, width .15s ease; }

    /* SỰ HIỆN DIỆN / RSVP */
    .rsvp { padding: 13vh 6vw; text-align: center; background: var(--cream); }
    .rsvp-inner { max-width: 660px; margin: 0 auto; }
    .rsvp h2 { font-family: var(--font-fraunces); font-weight: 400; color: var(--wine-deep); font-size: clamp(2.2rem, 5.5vw, 3.8rem); line-height: 1.02; margin: 14px 0 24px; }
    .rsvp h2 em { font-style: italic; color: var(--gold); }
    .rsvp-lead { color: #6a5348; font-weight: 300; line-height: 1.9; font-size: 15px; margin: 0 auto 36px; max-width: 46ch; }
    .rsvp-actions { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
    .rbtn { display: inline-flex; align-items: center; padding: 16px 34px; border-radius: 999px; font-size: 12px; letter-spacing: .22em; text-transform: uppercase; text-decoration: none; transition: background .4s, color .4s, transform .4s; }
    .rbtn:not(.ghost) { background: var(--wine); color: var(--cream); border: 1px solid var(--wine); }
    .rbtn:not(.ghost):hover { background: var(--wine-deep); transform: translateY(-3px); }
    .rbtn.ghost { border: 1px solid var(--gold); color: var(--wine); }
    .rbtn.ghost:hover { background: var(--gold); color: #fff; transform: translateY(-3px); }
    .rsvp-note { margin-top: 34px; font-family: var(--font-fraunces); font-style: italic; color: var(--gold); font-size: 1.15rem; }

    /* FOOTER */
    .foot { text-align: center; padding: 14vh 6vw 10vh; background: var(--wine-deep); color: var(--cream); }
    .foot-mono { font-family: var(--font-fraunces); font-style: italic; font-size: 64px; color: var(--gold); }
    .foot-thanks { max-width: 44ch; margin: 22px auto 40px; font-weight: 300; line-height: 1.9; opacity: .9; }
    .foot-names { font-family: var(--font-fraunces); font-style: italic; font-size: clamp(2rem, 6vw, 3.4rem); margin: 0; }
    .foot-date { letter-spacing: .3em; font-size: 12px; margin-top: 12px; opacity: .7; }

    /* VỎ THIỆP / COVER */
    .cover {
      position: fixed; inset: 0; z-index: 200; overflow: hidden;
      display: flex; align-items: center; justify-content: center; padding: 24px;
      background: var(--paper);
      transition: transform 1.1s cubic-bezier(.76, 0, .24, 1), opacity .8s ease .15s;
    }
    .cover-inner { flex: 0 1 auto; min-width: 0; }
    .cover::before {
      content: ''; position: absolute; inset: 0;
      background: url('/assets/concept-20.jpeg') center / cover;
      opacity: .12; filter: grayscale(.2);
    }
    .cover.open { transform: translateY(-105%); opacity: 0; pointer-events: none; }
    /* Đã có tên lưu sẵn → ẩn hẳn vỏ thiệp (không nháy) */
    .skip-cover .cover { display: none; }
    .cover-inner {
      position: relative; z-index: 1; box-sizing: border-box; text-align: center;
      width: min(440px, calc(100vw - 52px));
      padding: 52px 34px; background: rgba(251, 248, 241, .9); backdrop-filter: blur(2px);
      border: 1px solid var(--gold); outline: 1px solid var(--gold); outline-offset: 6px;
      animation: coverIn 1s cubic-bezier(.22, 1, .36, 1) both;
    }
    @keyframes coverIn { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: none; } }
    .cover-cross { display: block; width: 1px; height: 30px; background: var(--gold); margin: 0 auto 16px; position: relative; }
    .cover-cross::before { content: ''; position: absolute; top: 8px; left: -7px; width: 15px; height: 1px; background: var(--gold); }
    .cover-mono { font-family: var(--font-fraunces); font-style: italic; font-size: 56px; line-height: 1; color: var(--wine); }
    .cover-kicker { margin: 18px 0 10px; font-size: 11px; letter-spacing: .34em; text-transform: uppercase; color: var(--gold); }
    .cover-names { margin: 0; font-family: var(--font-fraunces); font-style: italic; font-weight: 400; font-size: clamp(2rem, 8vw, 2.8rem); line-height: 1.1; color: var(--wine-deep); }
    .cover-names em { color: var(--gold); font-style: italic; }
    .cover-date { margin: 12px 0 0; font-family: var(--font-fraunces); letter-spacing: .16em; font-size: 13px; text-transform: uppercase; color: var(--ink); }
    .cover-form { margin-top: 30px; display: flex; flex-direction: column; align-items: center; gap: 14px; }
    .cover-label { font-size: 11px; letter-spacing: .28em; text-transform: uppercase; color: #8a7565; }
    .cover-form input {
      width: 100%; text-align: center; border: 0; border-bottom: 1px solid var(--gold);
      background: transparent; padding: 9px 6px; font-family: var(--font-be); font-size: 16px; color: var(--ink); outline: none;
    }
    .cover-form input::placeholder { color: #b3a08f; }
    .cover-form input:focus { border-bottom-color: var(--wine); }
    .cover-open {
      margin-top: 8px; padding: 14px 32px; border-radius: 999px; border: 1px solid var(--wine);
      background: var(--wine); color: var(--cream); font-size: 12px; letter-spacing: .22em; text-transform: uppercase; cursor: pointer;
      transition: background .3s, transform .3s;
    }
    .cover-open:hover { background: var(--wine-deep); transform: translateY(-2px); }

    /* Lời chào cá nhân hoá trong hero */
    .guest-greet { font-family: var(--font-fraunces); font-style: italic; font-size: 17px; color: var(--gold); margin: 0 0 14px; }

    @media (max-width: 900px) {
      /* Thanh chip mảnh luôn hiện, cuộn ngang được */
      .side-nav { flex-direction: row; align-items: center; width: 100%; height: 52px; bottom: auto; padding: 0; background: rgba(74,13,22,.97); mix-blend-mode: normal; justify-content: flex-start; }
      .side-nav .brand { flex: 0 0 auto; padding: 0 14px; font-size: 22px; color: #fff; }
      .side-date { display: none; }
      .side-nav ul {
        flex: 1 1 auto; min-width: 0; height: 100%;
        flex-direction: row; flex-wrap: nowrap; align-items: center; gap: 8px;
        margin: 0; padding: 0 18px 0 2px; overflow-x: auto; overflow-y: hidden;
        scrollbar-width: none; -ms-overflow-style: none;
        -webkit-mask-image: linear-gradient(90deg, #000 86%, transparent);
        mask-image: linear-gradient(90deg, #000 86%, transparent);
      }
      .side-nav ul::-webkit-scrollbar { display: none; }
      .side-nav ul li { flex: 0 0 auto; }
      .side-nav ul a {
        writing-mode: horizontal-tb; text-transform: none; white-space: nowrap; display: block;
        padding: 7px 15px; border: 1px solid rgba(255,255,255,.28); border-radius: 999px;
        font-size: 12px; letter-spacing: .05em; color: #fff; opacity: .9;
      }
      .side-nav ul a:hover, .side-nav ul a:active { background: var(--gold); border-color: var(--gold); color: #fff; opacity: 1; }
      .hero { grid-template-columns: 1fr; padding-left: 0; padding-top: 52px; }
      .hero-copy { padding: 8vw 8vw 4vw; order: 2; }
      .hero-fig { height: 62vh; order: 1; }
      .hero-fig::after { background: linear-gradient(0deg, var(--paper) 0%, transparent 30%); }
      .scroll-cue { display: none; }
      .story, .events { padding-left: 8vw; padding-right: 8vw; }
      .story-row, .story-row.alt { grid-template-columns: 1fr; }
      .story-row.alt .sr-img { order: 0; }
      .slider-nav { display: none; } /* mobile: dùng vuốt, ẩn nút mũi tên */
      .slide { width: clamp(220px, 72vw, 320px); }
    }
    @media (max-width: 560px) {
      .slide { width: 78vw; }
      .count-grid { gap: 18px; }
      .cover-inner { padding: 42px 22px; }
      .cover-mono { font-size: 48px; }
    }
  </style>
</Base>
